import { REPORTS_CACHE_PROVIDER  } from './cacheable-events.config';

describe('CacheableEvents', () => {
  it('should create an instance', () => {
    expect(REPORTS_CACHE_PROVIDER).toBeTruthy();
  });
});
