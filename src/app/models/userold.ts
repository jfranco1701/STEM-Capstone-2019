import { EmailValidator } from '@angular/forms';

export class Userold {
    username: string;
    email: string;
    first_name: string;
    last_name: string;
    date_of_birth: string;
    organization: number;
    user_type: number;
    is_student: boolean;
}
